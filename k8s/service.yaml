apiVersion: v1
kind: Service
metadata:
  name: maintenance-copilot-api
  namespace: maintenance-copilot
  labels:
    app.kubernetes.io/name: maintenance-copilot
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: maintenance-copilot
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
